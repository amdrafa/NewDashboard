import { Flex } from "@chakra-ui/react";
import dayjs from "dayjs";

interface carouselProps {}

export function CardCarousel({}: carouselProps) {
  return (
    <Flex
      w="100%"
      height="200px"
      bg={"gray.500"}
      justifyContent="center"
      alignItems={"center"}
    >
      a
    </Flex>
  );
}
